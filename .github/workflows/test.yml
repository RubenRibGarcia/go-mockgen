name: test
on:
  push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      
      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: 'stable'
      
      - name: Generate
        run: go generate ./...
      - name: Test
        run: go test -race -v ./...
